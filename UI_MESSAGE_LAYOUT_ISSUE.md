# Web 消息布局问题说明

## 当前结论

上下文压缩消息和错误卡片的间距问题仍未解决。本轮代码修改已经通过格式检查、lint、TypeScript 检查和前端构建，但这些检查只能证明代码可构建，不能证明浏览器中的实际消息间距正确。因此当前实现不能视为问题已修复。

## 已观察到的问题

- 消息图标和文字在部分状态下没有稳定保持同行。
- 上下文压缩消息使用了不同于普通助手消息的结构和样式，导致对齐与间距不一致。
- HTTP 错误卡片与前后普通消息之间的上下间距不一致。
- 错误卡片后可能存在不可见但仍参与虚拟列表高度计算的 footer 占位。
- 强制刷新后，页面仍然表现为间距没有变化。

## 本轮尝试

- 给错误卡片补充普通助手消息行的 class，使其复用普通消息的宽度和 padding 规则。
- 给上下文压缩消息移除 `system-message-block` 特殊盒模型，使其复用普通助手消息行。
- 将上下文压缩标题按钮改为 flex 布局，并让按钮 label 使用 `display: contents`，保持图标、文字和箭头同行。
- 在存在 provider error 时不再追加不可见 round footer。
- 调整错误卡片和上下文压缩消息的虚拟列表高度估算。

## 为什么不能认为已经解决

当前消息由虚拟列表的 `.message-row` 外层、消息组件自身的 padding、渲染器内部的 margin，以及可能存在的隐藏 footer 共同决定高度。仅修改子组件 CSS 无法证明最终的 row 边界已经一致。

还需要对实际渲染结果逐项确认：

1. `.message-row` 的实际高度、起始位置和相邻 row 的间隔。
2. 普通助手消息、上下文压缩消息和错误卡片的 computed padding、margin、border box。
3. 错误卡片前后是否仍然存在不可见的消息项或占位高度。
4. 虚拟列表测量完成前后的高度是否发生额外跳变。
5. hover 状态是否被通用 `.ui-button` 规则覆盖。

## 当前提交范围

本次提交包含上述代码尝试和本说明文档。`CUA_INTEGRATION_PLAN.md` 是已有的未跟踪文件，不属于本次修改范围。
