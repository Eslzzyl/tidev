本文档记录在重写之后需要补充的功能

thinking 可折叠

优化提示词，特别是 plan/build 模式。现在有几个问题：

- 模型在 plan 模式下疯狂要求切换到 build。这两个词应该很少提及才对
- 模型对模式切换感知不明显，经常忽略
- 模型经常幻觉是系统自动发生了切换，或者它本身有能力触发这个切换。必须明确每次切换都只能是用户手动操作的。

添加禁词功能，检测到禁词就自动插入提示严禁使用禁词并且把禁词从消息中删了，首先要禁的就是 deepseek v4 flash 的“你说得对”

presets.toml 拆分

根据不同的模型插入可选的优化提示词

已实现：忙时发送模式（`send_while_busy`，排队/引导）——见 `architecture.md` 的"忙时消息调度"一节。
