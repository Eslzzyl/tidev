[package]
name = "tidev-notification"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "tidev desktop notification support (OSC 9 / BEL)"

[lib]
name = "tidev_notification"

[dependencies]
crossterm = "0.29.0"
log = "0.4.32"
tidev-config = { path = "../tidev-config" }
